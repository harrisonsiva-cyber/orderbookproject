#    sorted_buy = sorted(BUY_ORDERS[i], key=lambda x: x["price"])
#    print(sorted_buy)

 #   sorted_sell = sorted(SELL_ORDERS[i], key=lambda x: x["price"], reverse=True)
 #   print(sorted_sell)

#        buy_data = BUY_ORDERS
#        sell_data = SELL_ORDERS
#        headers = ["Order Number", "Order Type", "Order Side", "Price", "Quantity"]
#        print(tabulate(buy_data, headers=headers, tablefmt="grid"))
#        print(tabulate(sell_data, headers=headers, tablefmt="grid"))







#    print("\nBuy Orders: ")
 #   for my_order in BUY_ORDERS:
  #      print(my_order)

  #  print("\nSell Orders: ")
   # for my_order in SELL_ORDERS:
    #    print(my_order)

#    if side == ord.OrderSide.BUY:
       # buy_table = BeautifulTable()
        #buy_table.column_headers = ["Order Number", "Order Type", "Price", "Quantity"]
        #buy_table.append_row([order_number, order_type, price, quantity])
        #print(buy_table)

#    if side == ord.OrderSide.SELL:
        #sell_table = BeautifulTable()
        #sell_table.column_headers = ["Order Number", "Order Type", "Price", "Quantity"]
        #sell_table.append_row([order_number, order_type, price, quantity])
        #print(sell_table)